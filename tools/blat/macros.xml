<?xml version="1.0"?>
<macros>
    <token name="@WRAPPER_VERSION@">1.0</token>

    <xml name="titlesize_minmatch_minidentity_dna">
        <param argument="-tileSize" type="integer" value="11" min="6" max="18" label="Size of match that triggers an alignment" help="Usually between 8 and 12" />
        <param argument="-minMatch" type="integer" value="2" min="1" max="10" label="Number of tile matches">
            <validator type="in_range" min="1" />
        </param>
        <param argument="-minIdentity" type="integer" value="90" min="0" max="100" label="Minimum sequence identity (%)" />
    </xml>

    <xml name="titlesize_minmatch_minidentity_prot">
        <param argument="-tileSize" type="integer" value="5" min="3" max="8" label="Size of match that triggers an alignment" />
        <param argument="-minMatch" type="integer" value="1" min="1" max="10" label="Number of tile matches">
            <validator type="in_range" min="1" />
        </param>
        <param argument="-minIdentity" type="integer" value="25" min="0" max="100" label="Minimum sequence identity (%)" />
    </xml>

    <xml name="format_output">
        <option value="psl">Tab-separated format, no sequence (psl)</option>
        <option value="psl -noHead">Tab-separated format, no sequence, no header (psl -noHead)</option>
        <option value="pslx">Tab-separated format with sequence (pslx)</option>
        <option value="pslx -noHead">Tab-separated format with sequence, no header (pslx -noHead)</option>
        <option value="axt">Blastz-associated axt format (axt)</option>
        <option value="maf">Multiz-associated maf format (maf)</option>
        <option value="sim4">Similar to sim4 format (sim4)</option>
        <option value="wublast">Similar to WU-BLAST format (wublast)</option>
        <option value="blast">Similar to NCBI BLAST format (blast)</option>
        <option value="blast8">NCBI BLAST tabular format (blast8)</option>
        <option value="blast9">NCBI BLAST tabular format with comments (blast9)</option>
    </xml>

    <xml name="mask_type">
        <option value="">No masking</option>
        <option value="lower">Mask out lower cased sequence</option>
        <option value="upper">Mask out upper cased sequence</option>
    </xml>

    <xml name="citations">
        <citations>
            <citation type="doi">10.1101/gr.229202</citation>
        </citations>
    </xml>
</macros>
